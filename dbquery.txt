create table customer (
id serial  not null primary key,
Server: 127.0.0.1:3306
Database & user: u469595974_sbd
password: sbdkelas1234

name text not null, 
phonenum text  not null,
username text  not null,
password text not null,
role text not null default user
);

insert into customer (name,phonenum,username,password) values
('Frenzel Timothy', '081310695040', 'frenzelts', 'password'); 

create table field(
fieldnum int unique,
tipe text not null
);

insert into field values
(1,'Sintetis'),
(2,'Finil'),
(3,'Sintetis'),
(4,'Finil');

create table booking(
id int not null references customer (id),
tgl date not null,
start int not null,
end int not null,
duration int not null,
field int not null references field(fieldnum),
status varchar(30) not null default 'Waiting for Confirmation'
);

insert into booking (id, tgl, start, end, duration, field)
values (1, '2019-01-02',10, 12, 2, 1);

